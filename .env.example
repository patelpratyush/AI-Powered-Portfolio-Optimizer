# 🔒 Security Configuration (REQUIRED for production)
SECRET_KEY=your-ultra-secure-secret-key-here-minimum-32-characters
JWT_SECRET_KEY=your-jwt-secret-key-here-minimum-32-characters-different-from-secret-key

# 📊 Database Configuration
DATABASE_URL=postgresql://portfolio_user:portfolio_pass@localhost:5432/portfolio_optimizer

# 🚀 API Configuration
API_HOST=0.0.0.0
API_PORT=5000
FLASK_ENV=development

# 🤖 ML Configuration
MODEL_DIR=models/saved
TRAINING_TIMEOUT=1800
MAX_TRAINING_WORKERS=3

# 🌐 External APIs
YAHOO_FINANCE_TIMEOUT=10

# 🛡️ Rate Limiting
RATE_LIMIT_PER_MINUTE=100

# 💾 Redis Configuration
REDIS_URL=redis://localhost:6379/0

# 📝 Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# 🔐 JWT Configuration
JWT_ACCESS_TOKEN_EXPIRES=3600

# 🌍 CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 📧 Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true

# 📈 Production Overrides (uncomment for production)
# FLASK_ENV=production
# DEBUG=false
# RATE_LIMIT_PER_MINUTE=60
# LOG_LEVEL=WARNING

# 🔑 Security Notes:
# 1. Never commit the actual .env file to version control
# 2. Generate random 32+ character strings for SECRET_KEY and JWT_SECRET_KEY
# 3. Use different values for each environment (dev/staging/prod)
# 4. Rotate secrets regularly in production
# 5. Use a secret management service for production deployments